"""Primeagent MCP Client — REST API-based MCP server for operating Primeagent.

Standalone MCP server that connects to a running Primeagent instance via REST API.
Requires no internal Primeagent services — just a URL and credentials.

Usage:
    python -m wfx.mcp
    # or via console script:
    wfx-mcp
"""
